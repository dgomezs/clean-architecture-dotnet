version: "3.9"
services:
  db:
    image: "mcr.microsoft.com/mssql/server"
    ports:
      - "1433:1433"
    environment:
      SA_PASSWORD: "TodoList@Demo"
      ACCEPT_EULA: "Y"
    volumes:
      - ./db:/var/opt/mssql
